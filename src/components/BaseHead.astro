---
// src/components/BaseHead.astro
// Este componente maneja el contenido que va DENTRO de la etiqueta <head> de tus páginas.
// NO debe contener las etiquetas <head> y </head> por sí mismo.
interface Props {
    title: string;
    description: string;
}

const { title, description } = Astro.props;
---

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<meta name="generator" content={Astro.generator} />
<title>{title}</title>
<meta name="description" content={description} />

{/* --- INICIO: CÓDIGO DE VERIFICACIÓN DE GOOGLE SEARCH CONSOLE --- */}
{/* Esta es la ÚNICA etiqueta de verificación. Asegúrate de que el 'content' sea tu código real. */}
<meta name="google-site-verification" content="ViZoA1X0KxQKG785LJEiFlJa2iHkQk4yv76I-iLuPyA" />
{/* --- FIN: CÓDIGO DE VERIFICACIÓN DE GOOGLE SEARCH CONSOLE --- */}

{/* Aquí puedes añadir otras etiquetas meta, links a hojas de estilo, etc. */}